name: Sample Book App

on:
  push:
    branches: [ "Day2" ]
  workflow_dispatch:

jobs:
  build-docker-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print process name
        run: echo "Building sample-book-app.." 
  deploy-dev:
    runs-on: ubuntu-latest
    needs: [build-docker-image]
    steps:
      - uses: actions/checkout@v4
      - name: Print process name
        run: echo "Deployment to DEV environment.." 
  test-dev:
    runs-on: ubuntu-latest
    needs: [deploy-dev]
    steps:
      - uses: actions/checkout@v4
      - name: Print process name
        run: echo "Testing Sample Book App service on DEV environment..." 
  deploy-stg:
    runs-on: ubuntu-latest
    needs: [test-dev]
    steps:
      - uses: actions/checkout@v4
      - name: Print process name
        run: echo "Deployment to STG environment.." 
  test-stg:
    runs-on: ubuntu-latest
    needs: [deploy-stg]
    steps:
      - uses: actions/checkout@v4
      - name: Print process name
        run: echo "Testing Sample Book App service on STG environment..." 
  deploy-prd:
    runs-on: ubuntu-latest
    needs: [test-stg]
    steps:
      - uses: actions/checkout@v4
      - name: Print process name
        run: echo "Deployment to PRD environment.." 
  test-prd:
    runs-on: ubuntu-latest
    needs: [deploy-prd]
    steps:
      - uses: actions/checkout@v4
      - name: Print process name
        run: echo "Testing Sample Book App service on PRD environment..." 